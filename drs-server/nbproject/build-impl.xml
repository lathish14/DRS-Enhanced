<?xml version="1.0" encoding="UTF-8"?>
<project name="drs-server-impl" basedir="..">

    <property file="nbproject/private/private.properties"/>
    <property file="nbproject/project.properties"/>

    <property name="libs.abs" location="${basedir}/${libs.path}"/>

    <path id="libs.classpath">
        <fileset dir="${libs.abs}" includes="*.jar"/>
    </path>

    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <target name="init">
        <mkdir dir="${build.classes.dir}"/>
        <mkdir dir="${build.test.dir}"/>
        <mkdir dir="${dist.dir}"/>
    </target>

    <target name="compile" depends="init">
        <javac srcdir="${src.dir}" destdir="${build.classes.dir}"
               encoding="UTF-8" debug="true" includeantruntime="false"
               source="${javac.source}" target="${javac.target}">
            <classpath>
                <path refid="libs.classpath"/>
            </classpath>
        </javac>
        <copy todir="${build.classes.dir}">
            <fileset dir="${src.dir}" excludes="**/*.java"/>
        </copy>
    </target>

    <target name="jar" depends="compile">
        <jar destfile="${dist.jar}" basedir="${build.classes.dir}">
            <manifest>
                <attribute name="Main-Class" value="${main.class}"/>
            </manifest>
        </jar>
    </target>

    <target name="build" depends="jar"/>

    <target name="run" depends="compile">
        <java classname="${main.class}" fork="true">
            <classpath>
                <pathelement path="${build.classes.dir}"/>
                <path refid="libs.classpath"/>
            </classpath>
        </java>
    </target>

    <target name="default" depends="run"/>

    <target name="compile-test" depends="compile">
        <javac srcdir="${test.src.dir}" destdir="${build.test.dir}"
               encoding="UTF-8" debug="true" includeantruntime="false">
            <classpath>
                <pathelement path="${build.classes.dir}"/>
                <path refid="libs.classpath"/>
            </classpath>
        </javac>
    </target>

    <target name="test" depends="compile-test">
        <junit printsummary="yes" fork="true" haltonfailure="no">
            <classpath>
                <pathelement path="${build.classes.dir}"/>
                <pathelement path="${build.test.dir}"/>
                <path refid="libs.classpath"/>
            </classpath>
            <batchtest>
                <fileset dir="${test.src.dir}" includes="**/*Test.java"/>
            </batchtest>
            <formatter type="plain" usefile="false"/>
        </junit>
    </target>

</project>
